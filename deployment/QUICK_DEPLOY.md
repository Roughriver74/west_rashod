# Быстрый деплой West Rashod на 192.168.45.98

## ✅ Готово к деплою!

Сервер проверен и готов к работе.

## 🚀 Три простых шага

### 1. Настройте конфигурацию (1 минута)

```bash
cd /Users/evgenijsikunov/projects/west/west_rashod

# Создайте .env.prod из примера
cp deployment/.env.prod.example deployment/.env.prod

# Отредактируйте файл
nano deployment/.env.prod
```

**Измените только эти строки:**

```bash
POSTGRES_PASSWORD=ваш_надёжный_пароль_123
SECRET_KEY=ваш-очень-длинный-секретный-ключ-минимум-32-символа-123456
```

### 2. Запустите деплой (5-10 минут)

```bash
./deploy.sh 192.168.45.98
```

### 3. Откройте приложение

- **Frontend**: http://192.168.45.98
- **Backend API**: http://192.168.45.98:8005/docs
- **Логин**: admin / admin

---

## 📋 Что будет развернуто

- PostgreSQL 15 (порт 54330)
- Redis 7 (порт 6382)
- Backend FastAPI (порт 8005)
- Frontend React (порт 5178)
- Nginx reverse proxy (порт 80)

---

## 🔧 Полезные команды

### Просмотр статуса

```bash
ssh root@192.168.45.98
cd /opt/west_rashod
docker compose -f docker-compose.prod.yml ps
```

### Просмотр логов

```bash
ssh root@192.168.45.98 'cd /opt/west_rashod && docker compose -f docker-compose.prod.yml logs -f'
```

### Рестарт

```bash
ssh root@192.168.45.98 'cd /opt/west_rashod && docker compose -f docker-compose.prod.yml restart'
```

### Обновление

```bash
git pull origin main
./deploy.sh 192.168.45.98
```

---

## ⚠️ Важная информация

### 1С сервер недоступен

При проверке обнаружено, что 1С сервер (10.10.100.77) недоступен с production сервера.

**Решение:**
1. Проверьте, что 1С сервер включен
2. Проверьте сетевую доступность: `ssh root@192.168.45.98 'ping 10.10.100.77'`
3. Убедитесь, что файрвол не блокирует доступ

Без доступа к 1С синхронизация не будет работать, но приложение запустится.

---

## 📚 Дополнительная документация

- **Полная инструкция**: [DEPLOYMENT.md](../DEPLOYMENT.md)
- **Проект**: [CLAUDE.md](../CLAUDE.md)
- **Быстрый старт разработки**: [QUICK_START.md](../QUICK_START.md)

---

## 🆘 Нужна помощь?

1. Проверьте логи: `ssh root@192.168.45.98 'cd /opt/west_rashod && docker compose -f docker-compose.prod.yml logs'`
2. Проверьте [Troubleshooting](../DEPLOYMENT.md#troubleshooting) в DEPLOYMENT.md
3. Перезапустите: `ssh root@192.168.45.98 'cd /opt/west_rashod && docker compose -f docker-compose.prod.yml restart'`

---

**Время деплоя**: ~10 минут
**Последняя проверка сервера**: 2025-12-23
