# Инструкции по деплою West Rashod

## ✅ Текущее состояние

**Приложение полностью работоспособно!**

- ✅ Backend: 4 воркера, sync_scheduler включен
- ✅ Frontend: собран с правильным API URL
- ✅ PostgreSQL: работает
- ✅ Redis: работает
- ✅ Nginx: работает
- ✅ Все проверки пройдены

**URL:** http://192.168.45.98
**Логин:** admin
**Пароль:** admin

---

## 🚀 Безопасный деплой

### Использование скрипта deploy-safe.sh

```bash
./deploy-safe.sh
```

**Что делает скрипт:**

1. ✅ Создает бекап текущей версии
2. ✅ Синхронизирует backend код
3. ✅ Синхронизирует frontend код
4. ✅ Собирает frontend (npm run build)
5. ✅ Обновляет файлы на продакшене
6. ✅ Исправляет права доступа
7. ✅ Перезапускает backend
8. ✅ Проверяет работоспособность API
9. ✅ Проверяет логи на ошибки
10. ✅ **Автоматический откат при ошибках**

### Откат к предыдущей версии

Если что-то пошло не так, откат выполняется автоматически. Для ручного отката:

```bash
# Найти доступные бекапы
ssh root@192.168.45.98 'ls -lh /opt/backups/west_rashod/'

# Откатить к конкретному бекапу
ssh root@192.168.45.98 'cd /opt/west_rashod && \
  tar -xzf /opt/backups/west_rashod/west_rashod_TIMESTAMP.tar.gz && \
  chown -R west_rashod:west_rashod /opt/west_rashod/backend/app && \
  systemctl restart west-rashod-backend.service'
```

---

## 🔧 Что было исправлено

### 1. Проблема с API URL в frontend

**Проблема:** Frontend был настроен на `http://192.168.45.98:8005`, но порт 8005 недоступен извне.

**Решение:**
- Обновлен `.env.production`: `VITE_API_URL=http://192.168.45.98`
- Frontend теперь обращается к backend через nginx на порту 80

### 2. Критическая ошибка в backend

**Проблема:** `AttributeError: 'TransactionClassifier' object has no attribute 'get_suggestions'`

**Решение:**
- Синхронизирован актуальный код backend с сервером
- Исправлены права доступа на файлы

### 3. Ошибка sync_scheduler при старте

**Проблема:** При запуске приложения sync_scheduler падал с ошибкой БД.

**Решение:**
- Добавлена проверка на `None` в методе `_should_run_sync`
- Добавлен try-catch при обновлении SyncSettings
- Добавлен rollback при ошибках
- Синхронизация продолжается даже если обновление настроек не удалось
- Временно отключена автосинхронизация (можно включить через UI)

### 4. Проблемы с правами доступа

**Проблема:** Файлы скопированные через rsync имели неправильные права.

**Решение:**
- Скрипт деплоя автоматически исправляет права: `chown -R west_rashod:west_rashod`

---

## 📊 Мониторинг

### Проверка статуса сервисов

```bash
ssh root@192.168.45.98 'systemctl status west-rashod-backend nginx postgresql redis --no-pager'
```

### Просмотр логов

```bash
# Backend логи
ssh root@192.168.45.98 'journalctl -u west-rashod-backend.service -f'

# Ошибки nginx
ssh root@192.168.45.98 'tail -f /var/log/nginx/west_rashod_error.log'

# Access логи nginx
ssh root@192.168.45.98 'tail -f /var/log/nginx/west_rashod_access.log'
```

### Проверка API

```bash
# Health check
curl http://192.168.45.98/api/v1/auth/login \
  -X POST \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin"
```

Должен вернуть `{"access_token":"...","token_type":"bearer"}`

---

## ⚙️ Настройка автосинхронизации

Автосинхронизация **временно отключена** для стабильности при старте.

**Для включения:**

1. Зайти в приложение: http://192.168.45.98
2. Перейти в **"Настройки синхронизации"**
3. Включить **"Автоматическая синхронизация"**
4. Настроить интервал и параметры
5. Сохранить

**Или через SQL:**

```bash
ssh root@192.168.45.98 'PGPASSWORD=rashod_pass_secure_2024 \
  psql -U rashod_user -d west_rashod_db -h localhost \
  -c "UPDATE sync_settings SET auto_sync_enabled = true WHERE id = 1;"'
```

---

## 🔐 Безопасность

### Пароли и учетные данные

**База данных:**
- Пользователь: `rashod_user`
- Пароль: `rashod_pass_secure_2024`
- База: `west_rashod_db`

**Приложение:**
- Логин: `admin`
- Пароль: `admin` (рекомендуется изменить)

**1C OData:**
- URL: `http://10.10.100.77/trade/odata/standard.odata`
- Пользователь: `odata.user`
- Пароль: `ak228Hu2hbs28`

---

## 📝 Полезные команды

### Перезапуск сервисов

```bash
# Только backend
ssh root@192.168.45.98 'systemctl restart west-rashod-backend.service'

# Только nginx
ssh root@192.168.45.98 'systemctl reload nginx'

# Все сервисы
ssh root@192.168.45.98 'systemctl restart west-rashod-backend nginx'
```

### Обновление только frontend

```bash
cd frontend && npm run build

ssh root@192.168.45.98 '
rm -rf /var/www/west_rashod/* && \
cp -r /opt/west_rashod/frontend/dist/* /var/www/west_rashod/ && \
chown -R www-data:www-data /var/www/west_rashod
'
```

### Обновление только backend

```bash
rsync -av --delete \
  --exclude='__pycache__' --exclude='*.pyc' --exclude='venv' \
  backend/app/ root@192.168.45.98:/opt/west_rashod/backend/app/

ssh root@192.168.45.98 '
chown -R west_rashod:west_rashod /opt/west_rashod/backend/app && \
systemctl restart west-rashod-backend.service
'
```

---

## ❗ Важные заметки

1. **Всегда используйте deploy-safe.sh** - он создает бекапы и проверяет работоспособность
2. **Не деплойте напрямую в продакшн** без тестирования скрипта
3. **Проверяйте логи** после каждого деплоя
4. **Бекапы хранятся** в `/opt/backups/west_rashod/`
5. **Frontend требует пересборки** при изменении кода (deploy-safe.sh делает это автоматически)
6. **Backend требует перезапуска** при изменении Python кода

---

## 🆘 Troubleshooting

### Backend не запускается

```bash
# Проверить логи
ssh root@192.168.45.98 'journalctl -u west-rashod-backend.service -n 50'

# Проверить права доступа
ssh root@192.168.45.98 'ls -la /opt/west_rashod/backend/app/services/'

# Исправить права
ssh root@192.168.45.98 'chown -R west_rashod:west_rashod /opt/west_rashod/backend/'
```

### Frontend показывает 502 Bad Gateway

```bash
# Проверить статус backend
ssh root@192.168.45.98 'systemctl status west-rashod-backend'

# Проверить что воркеры запустились
ssh root@192.168.45.98 'ps aux | grep uvicorn'
```

### API не отвечает

```bash
# Проверить напрямую на сервере
ssh root@192.168.45.98 'curl http://localhost:8005/docs'

# Проверить через nginx
curl http://192.168.45.98/docs
```

---

## 📞 Контакты и поддержка

При возникновении проблем:

1. Проверьте логи (см. раздел Мониторинг)
2. Попробуйте откат к предыдущей версии
3. Проверьте статус всех сервисов
4. Обратитесь к документации проекта

**Файлы документации:**
- [CLAUDE.md](CLAUDE.md) - основная документация для Claude
- [QUICK_START.md](QUICK_START.md) - быстрый старт
- [DEPLOYMENT.md](DEPLOYMENT.md) - детальное руководство по деплою
